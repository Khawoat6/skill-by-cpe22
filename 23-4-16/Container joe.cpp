#include <iostream>

using namespace std;

int main () {
	string str;
	int count;
	
	while (true) {
		cin >> str;
		
		if (str == "end") {
			break;
		}/*
		else {
			char arr[str.length()];
			count = 0;
			
			for (int i = 0 ; i < str.length() ; i++) {
				arr[i] = str[i];
			}
			
			for (int i = 0 ; i < str.length()-1 ; i++) {
				if (arr[i] < arr[i+1]) {
					count += 1;
				}
			}
			
			cout << count << endl;
		}*/
	}
	
}
